{
  "name": "Build zipc",
  "steps": [
    {
      "name": "Build",
      "run": "zig build -Doptimize=ReleaseSafe",
      "capture_sizes": ["zig-out/lib/libzipc.a"]
    },
    {
      "name": "Test",
      "run": "zig build test"
    }
  ]
}
